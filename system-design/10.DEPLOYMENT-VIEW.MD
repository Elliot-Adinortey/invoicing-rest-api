```mermaid
flowchart LR
    User[API Consumer] --> Internet[HTTPS]
    Internet --> WebServer[Nginx / Apache]
    WebServer --> Laravel[Laravel App]
    Laravel --> DB[(MySQL / PostgreSQL)]
    Laravel --> Logs[Application Logs]
```
